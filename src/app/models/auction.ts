export interface Auction {
}

export interface players {
    name?: string;
    isSelected?: boolean;
    amount?: number;
    team?: string;
}
export interface teams {
    name?: string;
    credits?: number;
}

